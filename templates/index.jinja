<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - Order List</title>
</head>
    <body>
        <h1>Order List!</h1>
        {% for order in order_list %}
        <p>
            <a href={{"http://localhost:4999/orders/" + order_list.index(order)|string }}>{{ order.customer_name }}</a>
        </p>
        {% endfor %}
    </body>
</html>